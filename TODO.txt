Retina face built to run on webcam
	- Figure out why it's so slow
	- Build as a shared library?
	- Otherwise, just use as a submodule and skip the test file

Find a better facial recognition model to use

Build up dataset for each emotion
- Do we need anything more than cropped images?
- Train emotion model on dataset
- Need testing and training data
- Ideally have two models that can be switched out easily, one for autistic individuals and one for non-autistic individuals

CDR content

Build up the service to handle game requests

Math for picking which face to choose in the frame

Request for re-calibrating when the user goes out of the screen
